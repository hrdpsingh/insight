[package]
name = "probe"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.14", default-features = false, features = ["wayland", "wgpu", "tokio", "canvas"] }
sysinfo = "0.39.2"

[profile.release]
lto = true
codegen-units = 1
